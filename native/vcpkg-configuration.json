{
  "$comment": "Overlays use the manifest baseline: SDL carries multisample fixes, SDL_image corrects the PNG grey ramp, and static Windows FreeType registers SFNT font drivers. Recast pins the fork used by @recast-navigation/wasm.",
  "overlay-ports": [
    "./vcpkg-overlay-ports"
  ]
}
