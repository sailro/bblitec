diff --git a/Include/RmlUi/Core/ID.h b/Include/RmlUi/Core/ID.h
index 75b224a6..56e0e896 100644
--- a/Include/RmlUi/Core/ID.h
+++ b/Include/RmlUi/Core/ID.h
@@ -106,6 +106,7 @@ enum class PropertyId : uint8_t {
 	TextTransform,
 	WhiteSpace,
 	WordBreak,
+	OverflowWrap,
 	RowGap,
 	ColumnGap,
 	Cursor,
diff --git a/Source/Core/ElementStyle.cpp b/Source/Core/ElementStyle.cpp
index 385857e2..25d16fec 100644
--- a/Source/Core/ElementStyle.cpp
+++ b/Source/Core/ElementStyle.cpp
@@ -1249,6 +1249,8 @@ void ElementStyle::ComputeValue(Style::ComputedValues& values, float dp_ratio, V
 	case PropertyId::WhiteSpace:
 		values.white_space((WhiteSpace)p->Get<int>());
 		break;
+	case PropertyId::OverflowWrap:
+		break; // Resolved from the inherited property by ElementText.
 	case PropertyId::WordBreak:
 		values.word_break((WordBreak)p->Get<int>());
 		break;
diff --git a/Source/Core/ElementText.cpp b/Source/Core/ElementText.cpp
index fe1dd2e0..7986903a 100644
--- a/Source/Core/ElementText.cpp
+++ b/Source/Core/ElementText.cpp
@@ -245,6 +245,7 @@ bool ElementText::GenerateLine(String& line, int& line_length, float& line_width
 	const TextShapingContext text_shaping_context{computed.language(), computed.direction(), computed.font_kerning(), computed.letter_spacing()};
 	TextTransform text_transform_property = computed.text_transform();
 	WordBreak word_break = computed.word_break();
+	const bool overflow_wrap = GetProperty(PropertyId::OverflowWrap)->Get<int>() != 0;
 
 	FontEngineInterface* font_engine_interface = GetFontEngineInterface();
 
@@ -275,7 +276,7 @@ bool ElementText::GenerateLine(String& line, int& line_length, float& line_width
 
 			if (token_width > max_token_width)
 			{
-				if (word_break == WordBreak::BreakAll || (word_break == WordBreak::BreakWord && line.empty()))
+				if (word_break == WordBreak::BreakAll || ((word_break == WordBreak::BreakWord || overflow_wrap) && line.empty()))
 				{
 					// Try to break up the word
 					max_token_width = RoundDownToIntegerClamped(maximum_line_width - line_width);
diff --git a/Source/Core/StyleSheetSpecification.cpp b/Source/Core/StyleSheetSpecification.cpp
index 339219f4..e17b3116 100644
--- a/Source/Core/StyleSheetSpecification.cpp
+++ b/Source/Core/StyleSheetSpecification.cpp
@@ -367,6 +367,7 @@ void StyleSheetSpecification::RegisterDefaultProperties()
 	RegisterProperty(PropertyId::TextTransform, "text-transform", "none", true, true).AddParser("keyword", "none, capitalize, uppercase, lowercase");
 	RegisterProperty(PropertyId::WhiteSpace, "white-space", "normal", true, true).AddParser("keyword", "normal, pre, nowrap, pre-wrap, pre-line");
 	RegisterProperty(PropertyId::WordBreak, "word-break", "normal", true, true).AddParser("keyword", "normal, break-all, break-word");
+	RegisterProperty(PropertyId::OverflowWrap, "overflow-wrap", "normal", true, true).AddParser("keyword", "normal, break-word, anywhere");
 
 	RegisterProperty(PropertyId::RowGap, "row-gap", "0px", false, true).AddParser("length_percent").SetRelativeTarget(RelativeTarget::ContainingBlockHeight);
 	RegisterProperty(PropertyId::ColumnGap, "column-gap", "0px", false, true).AddParser("length_percent").SetRelativeTarget(RelativeTarget::ContainingBlockHeight);
