diff --git a/Include/RmlUi/Core/RenderBox.h b/Include/RmlUi/Core/RenderBox.h
index d08f9c82..6df2cf5d 100644
--- a/Include/RmlUi/Core/RenderBox.h
+++ b/Include/RmlUi/Core/RenderBox.h
@@ -7,7 +7,7 @@ namespace Rml {
 // Ordered by top, right, bottom, left.
 using EdgeSizes = Array<float, 4>;
 // Ordered by top-left, top-right, bottom-right, bottom-left.
-using CornerSizes = Array<float, 4>;
+using CornerSizes = Array<Vector2f, 4>;
 
 /**
     Provides the data needed to generate a mesh for a given element's box.
diff --git a/Include/RmlUi/Core/ComputedValues.h b/Include/RmlUi/Core/ComputedValues.h
index 3695a978..22c93d96 100644
--- a/Include/RmlUi/Core/ComputedValues.h
+++ b/Include/RmlUi/Core/ComputedValues.h
@@ -179,7 +179,7 @@ namespace Style {
 		float flex_basis = 0;
 		float row_gap = 0, column_gap = 0;
 
-		int16_t border_top_left_radius = 0, border_top_right_radius = 0, border_bottom_right_radius = 0, border_bottom_left_radius = 0;
+		BorderRadius border_top_left_radius, border_top_right_radius, border_bottom_right_radius, border_bottom_left_radius;
 		Colourb image_color = Colourb(255, 255, 255);
 		float scrollbar_margin = 0.f;
 	};
@@ -276,12 +276,12 @@ namespace Style {
 		float             flex_grow()                  const { return GetLocalProperty(PropertyId::FlexGrow, 0.f); }
 		float             flex_shrink()                const { return GetLocalProperty(PropertyId::FlexShrink, 1.f); }
 		FlexBasis         flex_basis()                 const { return LengthPercentageAuto(rare.flex_basis_type, rare.flex_basis); }
-		float             border_top_left_radius()     const { return (float)rare.border_top_left_radius; }
-		float             border_top_right_radius()    const { return (float)rare.border_top_right_radius; }
-		float             border_bottom_right_radius() const { return (float)rare.border_bottom_right_radius; }
-		float             border_bottom_left_radius()  const { return (float)rare.border_bottom_left_radius; }
-		CornerSizes       border_radius()              const { return {(float)rare.border_top_left_radius,     (float)rare.border_top_right_radius,
-		                                                               (float)rare.border_bottom_right_radius, (float)rare.border_bottom_left_radius}; }
+		BorderRadius             border_top_left_radius()     const { return rare.border_top_left_radius; }
+		BorderRadius             border_top_right_radius()    const { return rare.border_top_right_radius; }
+		BorderRadius             border_bottom_right_radius() const { return rare.border_bottom_right_radius; }
+		BorderRadius             border_bottom_left_radius()  const { return rare.border_bottom_left_radius; }
+		Array<BorderRadius, 4> border_radius()          const { return {rare.border_top_left_radius, rare.border_top_right_radius,
+		                                                               rare.border_bottom_right_radius, rare.border_bottom_left_radius}; }
 		TextOverflow      text_overflow()              const { return rare.text_overflow; }
 		String            text_overflow_string()       const { return GetLocalProperty(PropertyId::TextOverflow, String());; }
 		Clip              clip()                       const { return rare.clip; }
@@ -370,10 +370,10 @@ namespace Style {
 		void perspective               (float value)             { rare.perspective                = value; }
 		void has_local_perspective     (bool value)              { rare.has_local_perspective      = value; }
 		void has_local_transform       (bool value)              { rare.has_local_transform        = value; }
-		void border_top_left_radius    (float value)             { rare.border_top_left_radius     = (int16_t)value; }
-		void border_top_right_radius   (float value)             { rare.border_top_right_radius    = (int16_t)value; }
-		void border_bottom_right_radius(float value)             { rare.border_bottom_right_radius = (int16_t)value; }
-		void border_bottom_left_radius (float value)             { rare.border_bottom_left_radius  = (int16_t)value; }
+		void border_top_left_radius    (BorderRadius value)             { rare.border_top_left_radius     = value; }
+		void border_top_right_radius   (BorderRadius value)             { rare.border_top_right_radius    = value; }
+		void border_bottom_right_radius(BorderRadius value)             { rare.border_bottom_right_radius = value; }
+		void border_bottom_left_radius (BorderRadius value)             { rare.border_bottom_left_radius  = value; }
 		void text_overflow             (TextOverflow value)      { rare.text_overflow              = value; }
 		void clip                      (Clip value)              { rare.clip                       = value; }
 		void drag                      (Drag value)              { rare.drag                       = value; }
diff --git a/Include/RmlUi/Core/StyleTypes.h b/Include/RmlUi/Core/StyleTypes.h
index bccaa6da..db29cab1 100644
--- a/Include/RmlUi/Core/StyleTypes.h
+++ b/Include/RmlUi/Core/StyleTypes.h
@@ -18,6 +18,12 @@ namespace Style {
 		LengthPercentage(Type type, float value = 0) : type(type), value(value) {}
 	};
 
+	// Percentages resolve separately against the border box's width and height.
+	struct BorderRadius {
+		LengthPercentage::Type type = LengthPercentage::Length;
+		Vector2f value;
+	};
+
 	struct NumberAuto {
 		enum Type : uint8_t { Auto, Number } type = Number;
 		float value = 0;
diff --git a/Source/Core/StyleSheetSpecification.cpp b/Source/Core/StyleSheetSpecification.cpp
index 350ea1dd..b2603b43 100644
--- a/Source/Core/StyleSheetSpecification.cpp
+++ b/Source/Core/StyleSheetSpecification.cpp
@@ -297,10 +297,10 @@ void StyleSheetSpecification::RegisterDefaultProperties()
 	RegisterShorthand(ShorthandId::BorderLeft, "border-left", "border-left-width, border-left-color", ShorthandType::FallThrough);
 	RegisterShorthand(ShorthandId::Border, "border", "border-top, border-right, border-bottom, border-left", ShorthandType::RecursiveRepeat);
 
-	RegisterProperty(PropertyId::BorderTopLeftRadius, "border-top-left-radius", "0px", false, false).AddParser("length");
-	RegisterProperty(PropertyId::BorderTopRightRadius, "border-top-right-radius", "0px", false, false).AddParser("length");
-	RegisterProperty(PropertyId::BorderBottomRightRadius, "border-bottom-right-radius", "0px", false, false).AddParser("length");
-	RegisterProperty(PropertyId::BorderBottomLeftRadius, "border-bottom-left-radius", "0px", false, false).AddParser("length");
+	RegisterProperty(PropertyId::BorderTopLeftRadius, "border-top-left-radius", "0px", false, false).AddParser("length_percent");
+	RegisterProperty(PropertyId::BorderTopRightRadius, "border-top-right-radius", "0px", false, false).AddParser("length_percent");
+	RegisterProperty(PropertyId::BorderBottomRightRadius, "border-bottom-right-radius", "0px", false, false).AddParser("length_percent");
+	RegisterProperty(PropertyId::BorderBottomLeftRadius, "border-bottom-left-radius", "0px", false, false).AddParser("length_percent");
 	RegisterShorthand(ShorthandId::BorderRadius, "border-radius", "border-top-left-radius, border-top-right-radius, border-bottom-right-radius, border-bottom-left-radius", ShorthandType::Box);
 
 	RegisterProperty(PropertyId::Display, "display", "inline", false, true)
diff --git a/Source/Core/ElementStyle.cpp b/Source/Core/ElementStyle.cpp
index f11a1ecf..1e9dac9a 100644
--- a/Source/Core/ElementStyle.cpp
+++ b/Source/Core/ElementStyle.cpp
@@ -1075,6 +1075,13 @@ void ElementStyle::ComputeValue(Style::ComputedValues& values, float dp_ratio, V
 {
 	using namespace Style;
 
+	const auto compute_border_radius = [&] {
+		if (p->unit == Unit::PX && p->value.GetType() == Variant::VECTOR2)
+			return BorderRadius{LengthPercentage::Length, p->Get<Vector2f>()};
+		const auto radius = ComputeLengthPercentage(p, font_size, document_font_size, dp_ratio, vp_dimensions);
+		return BorderRadius{radius.type, Vector2f(radius.value)};
+	};
+
 	// clang-format off
 	switch (id)
 	{
@@ -1133,16 +1140,16 @@ void ElementStyle::ComputeValue(Style::ComputedValues& values, float dp_ratio, V
 		break;
 
 	case PropertyId::BorderTopLeftRadius:
-		values.border_top_left_radius(ComputeLength(p->GetNumericValue(), font_size, document_font_size, dp_ratio, vp_dimensions));
+		values.border_top_left_radius(compute_border_radius());
 		break;
 	case PropertyId::BorderTopRightRadius:
-		values.border_top_right_radius(ComputeLength(p->GetNumericValue(), font_size, document_font_size, dp_ratio, vp_dimensions));
+		values.border_top_right_radius(compute_border_radius());
 		break;
 	case PropertyId::BorderBottomRightRadius:
-		values.border_bottom_right_radius(ComputeLength(p->GetNumericValue(), font_size, document_font_size, dp_ratio, vp_dimensions));
+		values.border_bottom_right_radius(compute_border_radius());
 		break;
 	case PropertyId::BorderBottomLeftRadius:
-		values.border_bottom_left_radius(ComputeLength(p->GetNumericValue(), font_size, document_font_size, dp_ratio, vp_dimensions));
+		values.border_bottom_left_radius(compute_border_radius());
 		break;
 
 	case PropertyId::Display:
diff --git a/Source/Core/Element.cpp b/Source/Core/Element.cpp
index d58c8da9..ba95d82f 100644
--- a/Source/Core/Element.cpp
+++ b/Source/Core/Element.cpp
@@ -520,7 +520,15 @@ RenderBox Element::GetRenderBox(BoxArea fill_area, int index)
 	case BoxArea::Auto: RMLUI_ERROR;
 	}
 
-	return RenderBox{inner_size, box.offset, edge_sizes, meta->computed_values.border_radius()};
+	const Vector2f border_size = box.padding_size + box.box.GetFrameSize(BoxArea::Border);
+	const auto computed_radii = meta->computed_values.border_radius();
+	CornerSizes radii;
+	for (size_t i = 0; i < radii.size(); ++i)
+	{
+		const auto& radius = computed_radii[i];
+		radii[i] = Math::Max(radius.type == Style::LengthPercentage::Percentage ? radius.value * border_size * 0.01f : radius.value, Vector2f(0));
+	}
+	return RenderBox{inner_size, box.offset, edge_sizes, radii};
 }
 
 int Element::GetNumBoxes()
diff --git a/Source/Core/ElementUtilities.cpp b/Source/Core/ElementUtilities.cpp
index ea4ed30f..57a2b566 100644
--- a/Source/Core/ElementUtilities.cpp
+++ b/Source/Core/ElementUtilities.cpp
@@ -157,8 +157,8 @@ bool ElementUtilities::GetClippingRegion(Element* element, Rectanglei& out_clip_
 			{
 				const TransformState* transform_state = clipping_element->GetTransformState();
 				const Matrix4f* transform = (transform_state ? transform_state->GetTransform() : nullptr);
-				const bool has_border_radius = (clip_computed.border_top_left_radius() > 0.f || clip_computed.border_top_right_radius() > 0.f ||
-					clip_computed.border_bottom_right_radius() > 0.f || clip_computed.border_bottom_left_radius() > 0.f);
+				const bool has_border_radius = (clip_computed.border_top_left_radius().value != Vector2f(0) || clip_computed.border_top_right_radius().value != Vector2f(0) ||
+					clip_computed.border_bottom_right_radius().value != Vector2f(0) || clip_computed.border_bottom_left_radius().value != Vector2f(0));
 
 				// If the element has border-radius we always use a clip mask, since we can't easily predict if content is located on the curved
 				// region to be clipped. If the element has a transform we only use a clip mask when the content clips.
diff --git a/Source/Core/GeometryBackgroundBorder.h b/Source/Core/GeometryBackgroundBorder.h
index a0808439..a63e5a5c 100644
--- a/Source/Core/GeometryBackgroundBorder.h
+++ b/Source/Core/GeometryBackgroundBorder.h
@@ -11,7 +11,7 @@ class Box;
 using EdgeSizes = Array<float, 4>;
 
 // Ordered by top-left, top-right, bottom-right, bottom-left.
-using CornerSizes = Array<float, 4>;
+using CornerSizes = Array<Vector2f, 4>;
 using CornerSizes2 = Array<Vector2f, 4>;
 using CornerPositions = Array<Vector2f, 4>;
 
@@ -21,10 +21,10 @@ struct BorderMetrics {
 	CornerPositions positions_outer;
 	// Inner corner positions (e.g. at padding edge)
 	CornerPositions positions_inner;
-	// Curved borders are drawn as circles (outer border) and ellipses (inner border) around the centers.
+	// Curved borders are drawn as ellipses (outer and inner border) around the centers.
 	CornerPositions positions_circle_center;
 
-	// Radii of the outer edges, always circles.
+	// Two-axis radii of the outer edges.
 	CornerSizes outer_radii;
 	// Radii of the inner edges, 2-dimensional as these can be elliptic.
 	// The inner radii is effectively the (signed) distance from the circle center to the inner edge.
@@ -59,7 +59,7 @@ private:
 	// All draw operations place vertices in clockwise order.
 
 	// Draw the corner, delegate to the specific corner shape drawing function.
-	void DrawBackgroundCorner(Corner corner, Vector2f pos_inner, Vector2f pos_circle_center, float R, Vector2f r, ColourbPremultiplied color);
+	void DrawBackgroundCorner(Corner corner, Vector2f pos_inner, Vector2f pos_circle_center, Vector2f R, Vector2f r, ColourbPremultiplied color);
 
 	// Add a single point.
 	void DrawPoint(Vector2f pos, ColourbPremultiplied color);
@@ -81,7 +81,7 @@ private:
 	// Where 'next' corner means along the clockwise direction. This way we can easily fill the triangles of the edges in FillEdge().
 
 	// Draw the corner, delegate to the specific corner shape drawing function.
-	void DrawBorderCorner(Corner corner, Vector2f pos_outer, Vector2f pos_inner, Vector2f pos_circle_center, float R, Vector2f r,
+	void DrawBorderCorner(Corner corner, Vector2f pos_outer, Vector2f pos_inner, Vector2f pos_circle_center, Vector2f R, Vector2f r,
 		ColourbPremultiplied color0, ColourbPremultiplied color1);
 
 	// Draw a sharp border corner, ie. no border-radius. Does not produce any triangles.
@@ -89,11 +89,11 @@ private:
 
 	// Draw an arc along the outer edge (radius R), and an arc along the inner edge (two-axis radius r),
 	// spaced evenly between angles a0,a1 (inclusive). Connect them by triangles. Colors are interpolated.
-	void DrawArcArc(Vector2f pos_center, float R, Vector2f r, float a0, float a1, ColourbPremultiplied color0, ColourbPremultiplied color1,
+	void DrawArcArc(Vector2f pos_center, Vector2f R, Vector2f r, float a0, float a1, ColourbPremultiplied color0, ColourbPremultiplied color1,
 		int num_points);
 
 	// Draw an arc along the outer edge, and connect them by triangles to a point on the inner edge.
-	void DrawArcPoint(Vector2f pos_center, Vector2f pos_inner, float R, float a0, float a1, ColourbPremultiplied color0, ColourbPremultiplied color1,
+	void DrawArcPoint(Vector2f pos_center, Vector2f pos_inner, Vector2f R, float a0, float a1, ColourbPremultiplied color0, ColourbPremultiplied color1,
 		int num_points);
 
 	// Add triangles between the previous corner to another one specified by the index (possibly yet-to-be-drawn).
diff --git a/Source/Core/GeometryBackgroundBorder.cpp b/Source/Core/GeometryBackgroundBorder.cpp
index 6106b49b..28e7cae6 100644
--- a/Source/Core/GeometryBackgroundBorder.cpp
+++ b/Source/Core/GeometryBackgroundBorder.cpp
@@ -34,8 +34,7 @@ BorderMetrics GeometryBackgroundBorder::ComputeBorderMetrics(Vector2f outer_posi
 
 	// -- For curved borders, find the positions to draw ellipses around, and the scaled outer and inner radii --
 
-	const float sum_radius = (outer_radii_def[TOP_LEFT] + outer_radii_def[TOP_RIGHT] + outer_radii_def[BOTTOM_RIGHT] + outer_radii_def[BOTTOM_LEFT]);
-	const bool has_radius = (sum_radius > 1.f);
+	const bool has_radius = std::any_of(outer_radii_def.begin(), outer_radii_def.end(), [](Vector2f radius) { return radius.x > 0 && radius.y > 0; });
 
 	if (has_radius)
 	{
@@ -43,15 +42,15 @@ BorderMetrics GeometryBackgroundBorder::ComputeBorderMetrics(Vector2f outer_posi
 		outer_radii = outer_radii_def;
 
 		// Scale the radii such that we have no overlapping curves.
-		float scale_factor = FLT_MAX;
-		scale_factor = Math::Min(scale_factor, inner_size.x / (outer_radii[TOP_LEFT] + outer_radii[TOP_RIGHT]));       // Top
-		scale_factor = Math::Min(scale_factor, inner_size.y / (outer_radii[TOP_RIGHT] + outer_radii[BOTTOM_RIGHT]));   // Right
-		scale_factor = Math::Min(scale_factor, inner_size.x / (outer_radii[BOTTOM_RIGHT] + outer_radii[BOTTOM_LEFT])); // Bottom
-		scale_factor = Math::Min(scale_factor, inner_size.y / (outer_radii[BOTTOM_LEFT] + outer_radii[TOP_LEFT]));     // Left
+		float scale_factor = 1.f;
+		scale_factor = Math::Min(scale_factor, outer_size.x / (outer_radii[TOP_LEFT].x + outer_radii[TOP_RIGHT].x));       // Top
+		scale_factor = Math::Min(scale_factor, outer_size.y / (outer_radii[TOP_RIGHT].y + outer_radii[BOTTOM_RIGHT].y));   // Right
+		scale_factor = Math::Min(scale_factor, outer_size.x / (outer_radii[BOTTOM_RIGHT].x + outer_radii[BOTTOM_LEFT].x)); // Bottom
+		scale_factor = Math::Min(scale_factor, outer_size.y / (outer_radii[BOTTOM_LEFT].y + outer_radii[TOP_LEFT].y));     // Left
 		scale_factor = Math::Min(1.0f, scale_factor);
 
-		for (float& radius : outer_radii)
-			radius = Math::Round(radius * scale_factor);
+		for (Vector2f& radius : outer_radii)
+			radius *= scale_factor;
 
 		// Place the circle/ellipse centers
 		metrics.positions_circle_center = {
@@ -125,10 +124,10 @@ void GeometryBackgroundBorder::DrawBorder(const BorderMetrics& metrics, EdgeSize
 	}
 }
 
-void GeometryBackgroundBorder::DrawBackgroundCorner(Corner corner, Vector2f pos_inner, Vector2f pos_circle_center, float R, Vector2f r,
+void GeometryBackgroundBorder::DrawBackgroundCorner(Corner corner, Vector2f pos_inner, Vector2f pos_circle_center, Vector2f R, Vector2f r,
 	ColourbPremultiplied color)
 {
-	if (R == 0 || r.x <= 0 || r.y <= 0)
+	if (R.x <= 0 || R.y <= 0 || r.x <= 0 || r.y <= 0)
 	{
 		DrawPoint(pos_inner, color);
 	}
@@ -136,7 +135,7 @@ void GeometryBackgroundBorder::DrawBackgroundCorner(Corner corner, Vector2f pos_
 	{
 		const float a0 = float((int)corner + 2) * 0.5f * Math::RMLUI_PI;
 		const float a1 = float((int)corner + 3) * 0.5f * Math::RMLUI_PI;
-		const int num_points = GetNumPoints(R);
+		const int num_points = GetNumPoints(Math::Max(R.x, R.y));
 		DrawArc(pos_circle_center, r, a0, a1, color, color, num_points);
 	}
 }
@@ -190,23 +189,23 @@ void GeometryBackgroundBorder::FillBackground(int index_start)
 	}
 }
 
-void GeometryBackgroundBorder::DrawBorderCorner(Corner corner, Vector2f pos_outer, Vector2f pos_inner, Vector2f pos_circle_center, float R,
+void GeometryBackgroundBorder::DrawBorderCorner(Corner corner, Vector2f pos_outer, Vector2f pos_inner, Vector2f pos_circle_center, Vector2f R,
 	Vector2f r, ColourbPremultiplied color0, ColourbPremultiplied color1)
 {
 	const float a0 = float((int)corner + 2) * 0.5f * Math::RMLUI_PI;
 	const float a1 = float((int)corner + 3) * 0.5f * Math::RMLUI_PI;
 
-	if (R == 0)
+	if (R.x <= 0 || R.y <= 0)
 	{
 		DrawPointPoint(pos_outer, pos_inner, color0, color1);
 	}
 	else if (r.x > 0 && r.y > 0)
 	{
-		DrawArcArc(pos_circle_center, R, r, a0, a1, color0, color1, GetNumPoints(R));
+		DrawArcArc(pos_circle_center, R, r, a0, a1, color0, color1, GetNumPoints(Math::Max(R.x, R.y)));
 	}
 	else
 	{
-		DrawArcPoint(pos_circle_center, pos_inner, R, a0, a1, color0, color1, GetNumPoints(R));
+		DrawArcPoint(pos_circle_center, pos_inner, R, a0, a1, color0, color1, GetNumPoints(Math::Max(R.x, R.y)));
 	}
 }
 
@@ -226,10 +225,10 @@ void GeometryBackgroundBorder::DrawPointPoint(Vector2f pos_outer, Vector2f pos_i
 	}
 }
 
-void GeometryBackgroundBorder::DrawArcArc(Vector2f pos_center, float R, Vector2f r, float a0, float a1, ColourbPremultiplied color0,
+void GeometryBackgroundBorder::DrawArcArc(Vector2f pos_center, Vector2f R, Vector2f r, float a0, float a1, ColourbPremultiplied color0,
 	ColourbPremultiplied color1, int num_points)
 {
-	RMLUI_ASSERT(num_points >= 2 && R > 0 && r.x > 0 && r.y > 0);
+	RMLUI_ASSERT(num_points >= 2 && R.x > 0 && R.y > 0 && r.x > 0 && r.y > 0);
 
 	const int num_triangles = 2 * (num_points - 1);
 
@@ -265,10 +264,10 @@ void GeometryBackgroundBorder::DrawArcArc(Vector2f pos_center, float R, Vector2f
 	}
 }
 
-void GeometryBackgroundBorder::DrawArcPoint(Vector2f pos_center, Vector2f pos_inner, float R, float a0, float a1, ColourbPremultiplied color0,
+void GeometryBackgroundBorder::DrawArcPoint(Vector2f pos_center, Vector2f pos_inner, Vector2f R, float a0, float a1, ColourbPremultiplied color0,
 	ColourbPremultiplied color1, int num_points)
 {
-	RMLUI_ASSERT(R > 0 && num_points >= 2);
+	RMLUI_ASSERT(R.x > 0 && R.y > 0 && num_points >= 2);
 
 	const int offset_vertices = (int)vertices.size();
 	vertices.reserve(offset_vertices + num_points + 2);
diff --git a/Source/Core/GeometryBoxShadow.h b/Source/Core/GeometryBoxShadow.h
index e9c17442..20c6beb7 100644
--- a/Source/Core/GeometryBoxShadow.h
+++ b/Source/Core/GeometryBoxShadow.h
@@ -9,7 +9,6 @@ using RenderBoxList = Vector<RenderBox>;
 struct BoxShadowGeometryInfo {
 	ColourbPremultiplied background_color;
 	Array<ColourbPremultiplied, 4> border_colors;
-	CornerSizes border_radius;
 	Vector2i texture_dimensions;
 	Vector2f element_offset_in_texture;
 	RenderBoxList padding_render_boxes;
@@ -19,7 +18,7 @@ struct BoxShadowGeometryInfo {
 };
 inline bool operator==(const BoxShadowGeometryInfo& a, const BoxShadowGeometryInfo& b)
 {
-	return a.background_color == b.background_color && a.border_colors == b.border_colors && a.border_radius == b.border_radius &&
+	return a.background_color == b.background_color && a.border_colors == b.border_colors &&
 		a.texture_dimensions == b.texture_dimensions && a.element_offset_in_texture == b.element_offset_in_texture &&
 		a.padding_render_boxes == b.padding_render_boxes && a.border_render_boxes == b.border_render_boxes && a.shadow_list == b.shadow_list &&
 		a.opacity == b.opacity;
@@ -37,11 +36,10 @@ class GeometryBoxShadow {
 public:
 	/// Resolve the element's properties into its box geometry info.
 	/// @param[in] element The element to resolve.
-	/// @param[in] border_radius The border radius of the element.
 	/// @param[in] background_color The background colour of the element.
 	/// @param[in] border_colors The border colours of the element.
 	/// @param[in] opacity The computed opacity of the element.
-	static BoxShadowGeometryInfo Resolve(Element* element, const CornerSizes& border_radius, ColourbPremultiplied background_color,
+	static BoxShadowGeometryInfo Resolve(Element* element, ColourbPremultiplied background_color,
 		const Array<ColourbPremultiplied, 4>& border_colors, float opacity);
 
 	/// Generate the texture and geometry for a box shadow and including the element's background and border.
diff --git a/Source/Core/GeometryBoxShadow.cpp b/Source/Core/GeometryBoxShadow.cpp
index f9356ca6..888d5e28 100644
--- a/Source/Core/GeometryBoxShadow.cpp
+++ b/Source/Core/GeometryBoxShadow.cpp
@@ -12,7 +12,7 @@
 
 namespace Rml {
 
-BoxShadowGeometryInfo GeometryBoxShadow::Resolve(Element* element, const CornerSizes& border_radius, ColourbPremultiplied background_color,
+BoxShadowGeometryInfo GeometryBoxShadow::Resolve(Element* element, ColourbPremultiplied background_color,
 	const Array<ColourbPremultiplied, 4>& border_colors, float opacity)
 {
 	RMLUI_ZoneScoped;
@@ -84,7 +84,6 @@ BoxShadowGeometryInfo GeometryBoxShadow::Resolve(Element* element, const CornerS
 	BoxShadowGeometryInfo geometry_info;
 	geometry_info.background_color = background_color;
 	geometry_info.border_colors = border_colors;
-	geometry_info.border_radius = border_radius;
 	geometry_info.texture_dimensions = texture_dimensions;
 	geometry_info.element_offset_in_texture = element_offset_in_texture;
 	geometry_info.padding_render_boxes = std::move(padding_render_boxes);
@@ -172,18 +171,6 @@ void GeometryBoxShadow::GenerateTexture(CallbackTexture& out_shadow_texture, Geo
 			const float spread_distance = shadow.spread_distance.number;
 			const float blur_radius = shadow.blur_radius.number;
 
-			CornerSizes spread_radii = info.border_radius;
-			for (int i = 0; i < 4; i++)
-			{
-				float& radius = spread_radii[i];
-				float spread_factor = (inset ? -1.f : 1.f);
-				if (radius < spread_distance)
-				{
-					const float ratio_minus_one = (radius / spread_distance) - 1.f;
-					spread_factor *= 1.f + ratio_minus_one * ratio_minus_one * ratio_minus_one;
-				}
-				radius = Math::Max(radius + spread_factor * spread_distance, 0.f);
-			}
 
 			Mesh mesh_shadow;
 
@@ -193,6 +180,21 @@ void GeometryBoxShadow::GenerateTexture(CallbackTexture& out_shadow_texture, Geo
 				const float signed_spread_distance = (inset ? -spread_distance : spread_distance);
 				RenderBox render_box = (inset ? info.padding_render_boxes : info.border_render_boxes)[i];
 				render_box.SetFillSize(Math::Max(render_box.GetFillSize() + Vector2f(2.f * signed_spread_distance), Vector2f{0.001f}));
+				CornerSizes spread_radii = render_box.GetBorderRadius();
+				for (Vector2f& corner : spread_radii)
+				{
+					for (float* axis : {&corner.x, &corner.y})
+					{
+						float& radius = *axis;
+						float spread_factor = (inset ? -1.f : 1.f);
+						if (radius < spread_distance)
+						{
+							const float ratio_minus_one = (radius / spread_distance) - 1.f;
+							spread_factor *= 1.f + ratio_minus_one * ratio_minus_one * ratio_minus_one;
+						}
+						radius = Math::Max(radius + spread_factor * spread_distance, 0.f);
+					}
+				}
 				render_box.SetBorderRadius(spread_radii);
 				render_box.SetBorderOffset(render_box.GetBorderOffset() - Vector2f(signed_spread_distance));
 				MeshUtilities::GenerateBackground(mesh_shadow, render_box, shadow.color);
diff --git a/Source/Core/BoxShadowCache.cpp b/Source/Core/BoxShadowCache.cpp
index 6b8155c4..a82af951 100644
--- a/Source/Core/BoxShadowCache.cpp
+++ b/Source/Core/BoxShadowCache.cpp
@@ -89,8 +89,7 @@ SharedPtr<BoxShadowRenderable> BoxShadowCache::GetHandle(Element* element, const
 		computed.border_bottom_color().ToPremultiplied(),
 		computed.border_left_color().ToPremultiplied(),
 	};
-	const CornerSizes border_radius = computed.border_radius();
-	BoxShadowGeometryInfo geom_info = GeometryBoxShadow::Resolve(element, border_radius, background_color, border_colors, computed.opacity());
+	BoxShadowGeometryInfo geom_info = GeometryBoxShadow::Resolve(element, background_color, border_colors, computed.opacity());
 	return GetOrCreateBoxShadow(*render_manager, geom_info);
 }
 
diff --git a/Source/Core/BoxShadowHash.h b/Source/Core/BoxShadowHash.h
index fa3d8869..8029478d 100644
--- a/Source/Core/BoxShadowHash.h
+++ b/Source/Core/BoxShadowHash.h
@@ -86,7 +86,7 @@ struct hash<::Rml::RenderBox> {
 	size_t operator()(const ::Rml::RenderBox& box) const noexcept
 	{
 		using namespace ::Rml::Utilities;
-		static auto HashArray4 = [](const ::Rml::Array<float, 4>& arr) -> size_t {
+		static auto HashArray4 = [](const auto& arr) -> size_t {
 			size_t seed = 0;
 			for (const auto& v : arr)
 				HashCombine(seed, v);
@@ -115,11 +115,6 @@ struct hash<::Rml::BoxShadowGeometryInfo> {
 			HashCombine(seed, v);
 		}
 
-		for (const auto& v : in.border_radius)
-		{
-			HashCombine(seed, v);
-		}
-
 		HashCombine(seed, in.texture_dimensions);
 		HashCombine(seed, in.element_offset_in_texture);
 
