diff --git a/Include/RmlUi/Core/ID.h b/Include/RmlUi/Core/ID.h
index d28c3268..325724d2 100644
--- a/Include/RmlUi/Core/ID.h
+++ b/Include/RmlUi/Core/ID.h
@@ -156,6 +156,7 @@ enum class PropertyId : uint8_t {
 
 	RmlUi_Language,
 	RmlUi_Direction,
+	BackgroundClip,
 
 	NumDefinedIds,
 	FirstCustomId = NumDefinedIds,
diff --git a/Source/Core/StyleSheetSpecification.cpp b/Source/Core/StyleSheetSpecification.cpp
index b7eda4a5..0ebd2221 100644
--- a/Source/Core/StyleSheetSpecification.cpp
+++ b/Source/Core/StyleSheetSpecification.cpp
@@ -341,6 +341,8 @@ void StyleSheetSpecification::RegisterDefaultProperties()
 
 	// Need some work on this if we are to include images.
 	RegisterProperty(PropertyId::BackgroundColor, "background-color", "transparent", false, false).AddParser("color");
+	RegisterProperty(PropertyId::BackgroundClip, "background-clip", "border-box", false, false)
+		.AddParser("keyword", "border-box, padding-box, content-box");
 	RegisterShorthand(ShorthandId::Background, "background", "background-color", ShorthandType::FallThrough);
 
 	RegisterProperty(PropertyId::Color, "color", "white", true, false).AddParser("color");
diff --git a/Source/Core/ElementStyle.cpp b/Source/Core/ElementStyle.cpp
index 412f9ba2..dbb68f08 100644
--- a/Source/Core/ElementStyle.cpp
+++ b/Source/Core/ElementStyle.cpp
@@ -1355,6 +1355,7 @@ void ElementStyle::ComputeValue(Style::ComputedValues& values, float dp_ratio, V
 	// Unhandled properties. Must be manually retrieved with 'GetProperty()'.
 	case PropertyId::FillImage:
 	case PropertyId::CaretColor:
+	case PropertyId::BackgroundClip:
 		break;
 	// Invalid properties
 	case PropertyId::Invalid:
diff --git a/Source/Core/Element.cpp b/Source/Core/Element.cpp
index f4b656ac..eb394d56 100644
--- a/Source/Core/Element.cpp
+++ b/Source/Core/Element.cpp
@@ -1916,6 +1916,7 @@ void Element::OnPropertyChange(const PropertyIdSet& changed_properties)
 	// Dirty the background if it's changed.
 	if (border_radius_changed ||                                    //
 		changed_properties.Contains(PropertyId::BackgroundColor) || //
+		changed_properties.Contains(PropertyId::BackgroundClip) ||  //
 		changed_properties.Contains(PropertyId::Opacity) ||         //
 		changed_properties.Contains(PropertyId::ImageColor) ||      //
 		changed_properties.Contains(PropertyId::BoxShadow))         //
diff --git a/Source/Core/ElementBackgroundBorder.cpp b/Source/Core/ElementBackgroundBorder.cpp
index e3e76446..8ebd589e 100644
--- a/Source/Core/ElementBackgroundBorder.cpp
+++ b/Source/Core/ElementBackgroundBorder.cpp
@@ -135,7 +135,9 @@ void ElementBackgroundBorder::GenerateGeometry(Element* element)
 	{
 		// CSS paints the background under the border (`background-clip: border-box` is the initial value), so a
 		// translucent border composites over the element's own background rather than over what lies behind it.
-		MeshUtilities::GenerateBackground(mesh, element->GetRenderBox(BoxArea::Border, i), background_color);
+		const int clip = element->GetProperty(PropertyId::BackgroundClip)->Get<int>();
+		const BoxArea background_area = clip == 1 ? BoxArea::Padding : clip == 2 ? BoxArea::Content : BoxArea::Border;
+		MeshUtilities::GenerateBackground(mesh, element->GetRenderBox(background_area, i), background_color);
 		MeshUtilities::GenerateBackgroundBorder(mesh, element->GetRenderBox(BoxArea::Padding, i), ColourbPremultiplied(0, 0, 0, 0), border_colors.data());
 	}
 
