Paint backgrounds under the border (CSS initial background-clip: border-box),
keep a clipped box-shadow layer at its original size, shrink-to-fit an absolutely
positioned box with one horizontal offset (CSS 2.1 section 10.3.7) and collapse a
last descendant's bottom margin through its block parent, as Chromium does.

diff --git a/Source/Core/ElementBackgroundBorder.cpp b/Source/Core/ElementBackgroundBorder.cpp
index 409b1d8..e3e7644 100644
--- a/Source/Core/ElementBackgroundBorder.cpp
+++ b/Source/Core/ElementBackgroundBorder.cpp
@@ -16,6 +16,10 @@ ElementBackgroundBorder::ElementBackgroundBorder() {}
 
 void ElementBackgroundBorder::Render(Element* element)
 {
+	const Vector2i viewport = element->GetRenderManager()->GetViewport();
+	if (viewport != background_viewport && GetBackground(BackgroundType::BoxShadowAndBackgroundBorder))
+		background_dirty = true;
+	background_viewport = viewport;
 	if (background_dirty || border_dirty)
 	{
 		for (auto& background : backgrounds)
@@ -132,7 +136,12 @@ void ElementBackgroundBorder::GenerateGeometry(Element* element)
 	Mesh mesh = geometry.Release(Geometry::ReleaseMode::ClearMesh);
 
 	for (int i = 0; i < element->GetNumBoxes(); i++)
-		MeshUtilities::GenerateBackgroundBorder(mesh, element->GetRenderBox(BoxArea::Padding, i), background_color, border_colors.data());
+	{
+		// CSS paints the background under the border (`background-clip: border-box` is the initial value), so a
+		// translucent border composites over the element's own background rather than over what lies behind it.
+		MeshUtilities::GenerateBackground(mesh, element->GetRenderBox(BoxArea::Border, i), background_color);
+		MeshUtilities::GenerateBackgroundBorder(mesh, element->GetRenderBox(BoxArea::Padding, i), ColourbPremultiplied(0, 0, 0, 0), border_colors.data());
+	}
 
 	geometry = render_manager->MakeGeometry(std::move(mesh));
 }
diff --git a/Source/Core/ElementBackgroundBorder.h b/Source/Core/ElementBackgroundBorder.h
--- a/Source/Core/ElementBackgroundBorder.h
+++ b/Source/Core/ElementBackgroundBorder.h
@@ -34,6 +34,7 @@ private:
 
 	bool background_dirty = false;
 	bool border_dirty = false;
+	Vector2i background_viewport;
 
 	StableMap<BackgroundType, Background> backgrounds;
 };
diff --git a/Source/Core/GeometryBoxShadow.cpp b/Source/Core/GeometryBoxShadow.cpp
index 872e64f..bc36f8e 100644
--- a/Source/Core/GeometryBoxShadow.cpp
+++ b/Source/Core/GeometryBoxShadow.cpp
@@ -66,7 +66,10 @@ BoxShadowGeometryInfo GeometryBoxShadow::Resolve(Element* element, const Corner
 		Math::ExpandToPixelGrid(texture_region);
 
 		element_offset_in_texture = -texture_region.TopLeft();
-		texture_dimensions = Vector2i(texture_region.Size());
+		// The render manager clips saved layers to its viewport. Use the same
+		// dimensions for the cache key and quad so clipped pixels keep their
+		// original size instead of stretching across the requested shadow area.
+		texture_dimensions = Math::Min(Vector2i(texture_region.Size()), element->GetRenderManager()->GetViewport());
 	}
 
 	// Since we can reuse textures across multiple box shadows with the same properties,
@@ -98,7 +101,11 @@ void GeometryBoxShadow::GenerateTexture(CallbackTexture& out_shadow_texture, Geo
 
 	Mesh mesh = out_background_border_geometry.Release(Geometry::ReleaseMode::ClearMesh);
 	for (size_t i = 0; i < info.padding_render_boxes.size(); i++)
-		MeshUtilities::GenerateBackgroundBorder(mesh, info.padding_render_boxes[i], info.background_color, info.border_colors.data());
+	{
+		// The background paints under the border, as in ElementBackgroundBorder.
+		MeshUtilities::GenerateBackground(mesh, info.border_render_boxes[i], info.background_color);
+		MeshUtilities::GenerateBackgroundBorder(mesh, info.padding_render_boxes[i], ColourbPremultiplied(0, 0, 0, 0), info.border_colors.data());
+	}
 	out_background_border_geometry = render_manager.MakeGeometry(std::move(mesh));
 
 	// Callback for generating the box-shadow texture. Using a callback ensures that the texture can be regenerated at any time, for example if the
diff --git a/Source/Core/Layout/LayoutDetails.cpp b/Source/Core/Layout/LayoutDetails.cpp
index c39dbbf..351b0f8 100644
--- a/Source/Core/Layout/LayoutDetails.cpp
+++ b/Source/Core/Layout/LayoutDetails.cpp
@@ -254,8 +254,14 @@ float LayoutDetails::GetShrinkToFitWidth(Element* element, Vector2f containing_b
 	float shrink_to_fit_width = layout_box->GetShrinkToFitWidth();
 	if (containing_block.x >= 0)
 	{
-		const float available_width =
-			Math::Max(0.f, containing_block.x - box.GetSizeAcross(BoxDirection::Horizontal, BoxArea::Margin, BoxArea::Padding));
+		// CSS 2.1 section 10.3.7: for an absolutely positioned element the 'left' and 'right' values take part in the
+		// available width, so a box with one offset shrinks to fit the space beside that offset.
+		const auto& computed = element->GetComputedValues();
+		float inset_width = 0.f;
+		if (computed.position() == Style::Position::Absolute || computed.position() == Style::Position::Fixed)
+			inset_width = ResolveValue(computed.left(), containing_block.x) + ResolveValue(computed.right(), containing_block.x);
+		const float available_width = Math::Max(0.f,
+			containing_block.x - inset_width - box.GetSizeAcross(BoxDirection::Horizontal, BoxArea::Margin, BoxArea::Padding));
 		shrink_to_fit_width = Math::Min(shrink_to_fit_width, available_width);
 	}
 	return shrink_to_fit_width;
diff --git a/Source/Core/Layout/BlockContainer.cpp b/Source/Core/Layout/BlockContainer.cpp
index dcdd2590..77201233 100644
--- a/Source/Core/Layout/BlockContainer.cpp
+++ b/Source/Core/Layout/BlockContainer.cpp
@@ -16,6 +16,7 @@ BlockContainer::BlockContainer(ContainerBox* _parent_container, FloatedBoxSpace*
 {
 	RMLUI_ASSERT(element);
 	RMLUI_ASSERT(box.GetSize().x >= 0.f);
+	bottom_margin = MarginStrut(box.GetEdge(BoxArea::Margin, BoxEdge::Bottom));
 
 	if (!space)
 	{
@@ -36,6 +37,24 @@ bool BlockContainer::Close(BlockContainer* parent_block_container)
 	// Set this box's height, if necessary.
 	if (box.GetSize().y < 0)
 	{
+		// CSS 2.2 8.3.1: an auto-height in-flow block with no bottom
+		// border/padding shares its last block child's adjoining bottom margin.
+		// Export the margin to the parent flow instead of including it in height.
+		if (parent_block_container && min_height == 0.f &&
+			box.GetEdge(BoxArea::Padding, BoxEdge::Bottom) == 0.f &&
+			box.GetEdge(BoxArea::Border, BoxEdge::Bottom) == 0.f)
+		{
+			if (const LayoutBox* child = GetOpenLayoutBox())
+			{
+				if (const Box* child_box = child->GetIfBox())
+				{
+					const Vector2f child_margin = GetBottomMarginStrut(*child);
+					bottom_margin = {Math::Max(bottom_margin.x, child_margin.x), Math::Min(bottom_margin.y, child_margin.y)};
+					box_cursor -= child_box->GetEdge(BoxArea::Margin, BoxEdge::Bottom);
+					box.SetEdge(BoxArea::Margin, BoxEdge::Bottom, bottom_margin.x + bottom_margin.y);
+				}
+			}
+		}
 		float content_height = box_cursor;
 
 		if (!parent_block_container)
@@ -298,29 +317,30 @@ Vector2f BlockContainer::NextBoxPosition(const Box& child_box, Style::Clear clea
 			const float open_bottom_margin = open_box->GetEdge(BoxArea::Margin, BoxEdge::Bottom);
 			const float margin_sum = open_bottom_margin + child_top_margin;
 
-			// The collapsed margin size depends on the sign of each margin, according to CSS behavior. The margins have
-			// already been added to the 'box_position', so subtract their sum as needed.
-			const int num_negative_margins = int(child_top_margin < 0.f) + int(open_bottom_margin < 0.f);
-			switch (num_negative_margins)
-			{
-			case 0:
-				// Use the largest margin.
-				box_position.y += Math::Max(child_top_margin, open_bottom_margin) - margin_sum;
-				break;
-			case 1:
-				// Use the sum of the positive and negative margin. These are already added to the position, so do nothing.
-				break;
-			case 2:
-				// Use the most negative margin.
-				box_position.y += Math::Min(child_top_margin, open_bottom_margin) - margin_sum;
-				break;
-			}
+			// Keep the positive maximum and negative minimum separately when
+			// the preceding block exported margins from nested descendants.
+			const Vector2f previous = GetBottomMarginStrut(*block_box);
+			const Vector2f next = MarginStrut(child_top_margin);
+			const float collapsed = Math::Max(previous.x, next.x) + Math::Min(previous.y, next.y);
+			box_position.y += collapsed - margin_sum;
 		}
 	}
 
 	return box_position;
 }
 
+Vector2f BlockContainer::MarginStrut(float margin)
+{
+	return {Math::Max(0.f, margin), Math::Min(0.f, margin)};
+}
+
+Vector2f BlockContainer::GetBottomMarginStrut(const LayoutBox& child)
+{
+	if (child.GetType() == Type::BlockContainer)
+		return static_cast<const BlockContainer&>(child).bottom_margin;
+	return MarginStrut(child.GetIfBox()->GetEdge(BoxArea::Margin, BoxEdge::Bottom));
+}
+
 void BlockContainer::PlaceQueuedFloats(float vertical_position)
 {
 	if (!queued_float_elements.empty())
diff --git a/Source/Core/Layout/BlockContainer.h b/Source/Core/Layout/BlockContainer.h
index 6cfb0a04..c8b1aa0d 100644
--- a/Source/Core/Layout/BlockContainer.h
+++ b/Source/Core/Layout/BlockContainer.h
@@ -109,6 +109,8 @@ private:
 	const InlineContainer* GetOpenInlineContainer() const;
 
 	const LayoutBox* GetOpenLayoutBox() const;
+	static Vector2f MarginStrut(float margin);
+	static Vector2f GetBottomMarginStrut(const LayoutBox& child);
 
 	/// Increment our cursor and content size, to enclose a sized block-level child box.
 	/// @param[in] child The child box.
@@ -146,6 +148,8 @@ private:
 
 	// The vertical position of the next block box to be added to this box, relative to our box's top content edge.
 	float box_cursor = 0.f;
+	// Positive maximum and negative minimum of adjoining bottom margins.
+	Vector2f bottom_margin;
 
 	// Stores the floated boxes in the current block formatting context, if we are the root of the formatting context.
 	UniquePtr<FloatedBoxSpace> root_space;
