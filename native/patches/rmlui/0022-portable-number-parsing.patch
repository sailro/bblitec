Parse CSS numbers through one locale-independent helper that uses Boost.Charconv
where the Android and macOS C++ libraries lack floating-point std::from_chars.

diff --git a/CMake/Dependencies.cmake b/CMake/Dependencies.cmake
index 23724bc..b1b18b3 100644
--- a/CMake/Dependencies.cmake
+++ b/CMake/Dependencies.cmake
@@ -105,3 +105,7 @@ if(RMLUI_TRACY_PROFILING)
 		endif()
 	endif()
 endif()
+
+if(ANDROID OR APPLE)
+    find_package(boost_charconv CONFIG REQUIRED)
+endif()
diff --git a/Source/Core/CMakeLists.txt b/Source/Core/CMakeLists.txt
index 092d736..09af90c 100644
--- a/Source/Core/CMakeLists.txt
+++ b/Source/Core/CMakeLists.txt
@@ -474,3 +474,7 @@ install(TARGETS rmlui_core
 	ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
 )
 install_target_pdb(rmlui_core)
+
+if(ANDROID OR APPLE)
+    target_link_libraries(rmlui_core PRIVATE Boost::charconv)
+endif()
diff --git a/Source/Core/ElementBackgroundBorder.cpp b/Source/Core/ElementBackgroundBorder.cpp
index e1b3fd8..ad44c2e 100644
--- a/Source/Core/ElementBackgroundBorder.cpp
+++ b/Source/Core/ElementBackgroundBorder.cpp
@@ -13,7 +13,7 @@
 #include "../../Include/RmlUi/Core/ElementUtilities.h"
 #include "../../Include/RmlUi/Core/URL.h"
 #include <algorithm>
-#include <charconv>
+#include "NumberParsing.h"
 #include <cmath>
 #include <sstream>
 
@@ -35,7 +35,7 @@ inline bool ReadLength(std::istream& input, Length& result)
     } else if (!token.empty() && token.back() == '%') {
         token.pop_back(); result.unit = LengthUnit::Percent;
     }
-    const auto parsed = std::from_chars(token.data(), token.data() + token.size(), result.value);
+    const auto parsed = number_parsing::from_chars(token.data(), token.data() + token.size(), result.value);
     return parsed.ec == std::errc{} && parsed.ptr == token.data() + token.size() &&
         std::isfinite(result.value) && result.value >= 0;
 }
diff --git a/Source/Core/Layout/GridTrackSizing.h b/Source/Core/Layout/GridTrackSizing.h
index 596f3b5..1dd4d61 100644
--- a/Source/Core/Layout/GridTrackSizing.h
+++ b/Source/Core/Layout/GridTrackSizing.h
@@ -2,7 +2,7 @@
 
 #include "../../../Include/RmlUi/Core/Types.h"
 #include <algorithm>
-#include <charconv>
+#include "../NumberParsing.h"
 #include <cctype>
 #include <cmath>
 #include <numeric>
@@ -53,7 +53,7 @@ private:
 	}
 	float Number() {
 		float value = 0.f;
-		const auto parsed = std::from_chars(input.data(), input.data() + input.size(), value);
+		const auto parsed = number_parsing::from_chars(input.data(), input.data() + input.size(), value);
 		if (parsed.ec != std::errc{} || !std::isfinite(value) || value < 0.f) Fail();
 		input.remove_prefix(size_t(parsed.ptr - input.data()));
 		return value;
@@ -92,7 +92,7 @@ private:
 			if (Take("repeat(")) {
 				if (repeated) Fail();
 				Space(); unsigned count_value = 0;
-				const auto parsed = std::from_chars(input.data(), input.data() + input.size(), count_value);
+				const auto parsed = number_parsing::from_chars(input.data(), input.data() + input.size(), count_value);
 				if (parsed.ec != std::errc{} || count_value < 1 || count_value > 256) Fail();
 				input.remove_prefix(size_t(parsed.ptr - input.data()));
 				Space(); if (!Take(",")) Fail();
diff --git a/Source/Core/NumberParsing.h b/Source/Core/NumberParsing.h
new file mode 100644
index 0000000..8bc4d45
--- /dev/null
+++ b/Source/Core/NumberParsing.h
@@ -0,0 +1,8 @@
+#pragma once
+#if defined(__ANDROID__) || defined(__APPLE__)
+#include <boost/charconv/from_chars.hpp>
+namespace Rml { namespace number_parsing = boost::charconv; }
+#else
+#include <charconv>
+namespace Rml { namespace number_parsing = std; }
+#endif
diff --git a/Source/Core/StyleSheetFactory.cpp b/Source/Core/StyleSheetFactory.cpp
index bebeddc..169cd0e 100644
--- a/Source/Core/StyleSheetFactory.cpp
+++ b/Source/Core/StyleSheetFactory.cpp
@@ -1,5 +1,5 @@
 #include "StyleSheetFactory.h"
-#include <charconv>
+#include "NumberParsing.h"
 #include <cmath>
 #include "../../Include/RmlUi/Core/Log.h"
 #include "../../Include/RmlUi/Core/StyleSheetContainer.h"
@@ -125,7 +125,7 @@ StructuralSelector StyleSheetFactory::GetSelector(const String& name)
 
         if (selector_type == StructuralSelectorType::ContainerMaxWidth) {
             float width = 0.f;
-            const auto parsed = std::from_chars(parameters.data(), parameters.data() + parameters.size(), width);
+            const auto parsed = number_parsing::from_chars(parameters.data(), parameters.data() + parameters.size(), width);
             if (parsed.ec != std::errc{} || parsed.ptr != parameters.data() + parameters.size() || !std::isfinite(width) || width < 0.f)
                 return StructuralSelector(StructuralSelectorType::Invalid, 0, 0);
             StructuralSelector result(selector_type, name.find("bbl-container-self-") == 0 ? 1 : 0, 0);
