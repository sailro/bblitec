diff --git a/Source/Core/Context.cpp b/Source/Core/Context.cpp
--- a/Source/Core/Context.cpp
+++ b/Source/Core/Context.cpp
@@ -1440,7 +1440,7 @@
 	}
 
 	// Ignore elements whose pointer events are disabled.
-	if (element->GetComputedValues().pointer_events() == Style::PointerEvents::None)
+	if (!element->IsVisible(true) || element->GetComputedValues().pointer_events() == Style::PointerEvents::None)
 		return nullptr;
 
 	// Projection may fail if we have a singular transformation matrix.
diff --git a/Source/Core/Element.cpp b/Source/Core/Element.cpp
--- a/Source/Core/Element.cpp
+++ b/Source/Core/Element.cpp
@@ -195,7 +195,7 @@
 
 	meta->effects.RenderEffects(RenderStage::Enter);
 
-	if (ElementUtilities::SetClippingRegion(this))
+	if (IsVisible() && ElementUtilities::SetClippingRegion(this))
 	{
 		meta->background_border.Render(this);
 		meta->effects.RenderEffects(RenderStage::Decoration);
@@ -568,12 +568,12 @@
 
 bool Element::IsVisible(bool include_ancestors) const
 {
-	if (!include_ancestors)
+	if (!visible || !include_ancestors)
 		return visible;
 	const Element* element = this;
 	while (element)
 	{
-		if (!element->visible)
+		if (element->GetComputedValues().display() == Style::Display::None)
 			return false;
 		element = element->parent;
 	}
@@ -1182,6 +1182,8 @@
 bool Element::Focus(bool focus_visible)
 {
 	// Are we allowed focus?
+	if (!IsVisible(true))
+		return false;
 	Style::Focus focus_property = meta->computed_values.focus();
 	if (focus_property == Style::Focus::None)
 		return false;
@@ -2398,7 +2400,7 @@
 {
 	using Style::Display;
 
-	if (!IsVisible())
+	if (GetDisplay() == Display::None)
 		return;
 
 	const Display display = GetDisplay();
diff --git a/Source/Core/ElementAnimation.cpp b/Source/Core/ElementAnimation.cpp
--- a/Source/Core/ElementAnimation.cpp
+++ b/Source/Core/ElementAnimation.cpp
@@ -698,6 +698,12 @@
 
 float ElementAnimation::GetInterpolationFactorAndKeys(int* out_key0, int* out_key1) const
 {
+	if (duration == 0.0f && keys.size() >= 2)
+	{
+		if (out_key0) *out_key0 = 0;
+		if (out_key1) *out_key1 = int(keys.size()) - 1;
+		return animation_complete ? 1.0f : 0.0f;
+	}
 	float t = time_since_iteration_start;
 
 	if (reverse_direction)
diff --git a/Source/Core/ElementDocument.cpp b/Source/Core/ElementDocument.cpp
--- a/Source/Core/ElementDocument.cpp
+++ b/Source/Core/ElementDocument.cpp
@@ -29,8 +29,10 @@
 
 	CanFocus CanFocusElement(Element* element)
 	{
+		if (element->GetDisplay() == Style::Display::None)
+			return CanFocus::NoAndNoChildren;
 		if (!element->IsVisible())
-			return CanFocus::NoAndNoChildren;
+			return CanFocus::No;
 
 		const ComputedValues& computed = element->GetComputedValues();
 
diff --git a/Source/Core/ElementStyle.cpp b/Source/Core/ElementStyle.cpp
--- a/Source/Core/ElementStyle.cpp
+++ b/Source/Core/ElementStyle.cpp
@@ -909,6 +909,7 @@
 		values.CopyInherited(*parent_values);
 	else if (!values_are_default_initialized)
 		values.CopyInherited(DefaultComputedValues());
+	values.visibility(parent_values ? parent_values->visibility() : Style::Visibility::Visible);
 
 	SmallUnorderedSet<String> variable_dependencies;
 	bool dirty_em_properties = false;
diff --git a/Source/Core/PropertyParserAnimation.cpp b/Source/Core/PropertyParserAnimation.cpp
--- a/Source/Core/PropertyParserAnimation.cpp
+++ b/Source/Core/PropertyParserAnimation.cpp
@@ -320,7 +320,7 @@
 		// Validate the parsed transition
 		if ((transition_list.all && !target_property_ids.Empty())    //
 			|| (!transition_list.all && target_property_ids.Empty()) //
-			|| transition.duration <= 0.0f                           //
+			|| transition.duration < 0.0f                            //
 			|| transition.reverse_adjustment_factor < 0.0f           //
 			|| transition.reverse_adjustment_factor > 1.0f           //
 		)
diff --git a/Source/Core/StyleSheetSpecification.cpp b/Source/Core/StyleSheetSpecification.cpp
--- a/Source/Core/StyleSheetSpecification.cpp
+++ b/Source/Core/StyleSheetSpecification.cpp
@@ -336,7 +336,7 @@
 	RegisterProperty(PropertyId::OverflowY, "overflow-y", "visible", false, true).AddParser("keyword", "visible, hidden, auto, scroll");
 	RegisterShorthand(ShorthandId::Overflow, "overflow", "overflow-x, overflow-y", ShorthandType::Replicate);
 	RegisterProperty(PropertyId::Clip, "clip", "auto", false, false).AddParser("keyword", "auto, none, always").AddParser("number");
-	RegisterProperty(PropertyId::Visibility, "visibility", "visible", false, false).AddParser("keyword", "visible, hidden");
+	RegisterProperty(PropertyId::Visibility, "visibility", "visible", true, false).AddParser("keyword", "visible, hidden");
 	RegisterProperty(PropertyId::TextOverflow, "text-overflow", "clip", false, false).AddParser("keyword", "clip, ellipsis").AddParser("string");
 
 	RegisterProperty(PropertyId::BorderImage, "border-image", "none", false, false).AddParser("string");
